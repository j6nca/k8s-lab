apiVersion: v1
kind: Secret
metadata:
  name: blahblahblahtest
  namespace: network
stringData:
  credentials.json: |-
    {
      "AccountTag": "blah",
      "TunnelID": "blah",
      "TunnelSecret": "blah"
    }
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: cloudflared-tunnel-credentials
  namespace: network
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    creationPolicy: Owner

    # this is how the Kind=Secret will look like
    template:
      type: kubernetes.io/tls
      data:
        # multiline string
        config: |-
          {
            "AccountTag": "{{ .account-id | toString }}",
            "TunnelID": "{{ .tunnel-id | toString }}",
            "TunnelSecret": "{{ .tunnel-secret | toString }}"
          }

  data:
  - secretKey: account-id
    remoteRef:
      key: cloudflared-tunnel-credentials
      property: account-id
  - secretKey: tunnel-id
    remoteRef:
      key: cloudflared-tunnel-credentials
      property: tunnel-id
  - secretKey: tunnel-secret
    remoteRef:
      key: cloudflared-tunnel-credentials
      property: tunnel-secret
